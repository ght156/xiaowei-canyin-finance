# 复制本文件为 backend/.env 并修改（.env 不会被提交到仓库）

# JWT 签名密钥：生产环境务必改成随机长字符串
# 生成方法：python -c "import secrets; print(secrets.token_hex(32))"
APP_SECRET_KEY=change-me-to-a-long-random-string

# 登录令牌有效期（天）
APP_TOKEN_EXPIRE_DAYS=7

# 备份保留份数
APP_BACKUP_KEEP=30

# 允许的跨域来源（逗号分隔，* 为全部）
# APP_CORS_ORIGINS=*
